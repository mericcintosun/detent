# DELTA (phase 8)

Read first: `IDENTITY.md` (D11 museum, L6 rail, M4 wipe, RADIUS sharp) and all
nine BEFORE shots under
`/Users/mericcintosun/farm-hackathon/data/exports/ethglobal-ethonline-2026/shots-faz8-mtxw0c1o/`.
Every change below moves existing markup onto tokens that already exist. No new
colour, font, radius or motion value is introduced anywhere in this phase.

## One-mark proof

`public/brand/logo.png` is the mark and `components/rail.tsx` renders it exactly
once, inside the home `Link`, as one `<Image>` plus one text node:

```tsx
<Link
  href="/"
  aria-current="page"
  className="flex items-center gap-3 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
>
  <Image
    src="/brand/logo.png"
    alt=""
    width={44}
    height={44}
    priority
    className="h-11 w-11 object-contain"
  />
  <span className="font-display text-2xl tracking-tight">Detent</span>
</Link>
```

No second brand image exists in the file, `public/brand/og.png` is imported by no
component, and no SVG wordmark was added. The raster reads as a gold seal on
parchment, which is on identity, so no Marka rerun is requested.

## Diagnosis

1. SHOT `before-mobile-fold.png` | region: the whole 390px first screen | defect: at phone width the viewport holds nothing but rail furniture on bone `#f4f1ea`: the mark, a three line blurb, six wide-tracked labels wrapped over two rows, a full-bleed gold `#ad8c3a` CTA block, then the four row network list. The product, the register masthead "Bosphorus Mercantile Equity", only begins at the very bottom edge of that screen and its numbers are roughly 1150 CSS pixels down. | source: `components/rail.tsx:26` (`flex h-full flex-col gap-8 px-6 py-6`), `components/rail.tsx:43` (blurb), `components/rail.tsx:50` (wrapping nav), `components/rail.tsx:72` (the four row `dl`) | violates: IDENTITY.md ARCHETYPE L6 rail, "the shell itself is the product", plus the phase goal that a judge sees the product in the first ten seconds at phone width | fixed by: C1, C2, C3
2. SHOT `before-desktop-fold.png` | region: the plan card header, "Distribute quarterly coupon" and the paragraph under it | defect: the repeating ledger rule runs straight through the display-face title and through both lines of the description, so the text reads as struck through with bone-shadow `#e2dccd` lines. | source: `components/operations-console.tsx:479` (`detent-ruled border-b border-border` on a `CardHeader` that renders `CardTitle` and `CardDescription`) | violates: IDENTITY.md "hairline rules ... the way a transfer agent's ledger page reads"; a rule under running text is a strikethrough, not a rule | fixed by: C4
3. SHOT `before-desktop-fold.png` | region: top right of the fold, the "REGISTER NOTE" block | defect: the note is a filled parchment `#ece7dc` panel with a full hairline border while the h1 beside it sits on bare bone, so the provenance block is the heaviest object on the fold and outranks the name of the security. Same inversion at 390px in `before-mobile-page-1.png`, where the filled panel is taller than the h1. | source: `components/operations-console.tsx:428` (`detent-enter border border-border bg-card p-4`) | violates: IDENTITY.md "Gold is spent on the rules"; the masthead h1 is the first read, a provenance note is not | fixed by: C5
4. SHOT `before-desktop-page-2.png` | region: "Lock this plan to the treasury key" in the quorum card, and "Execute the approved plan" at the foot of the send card | defect: both gold primaries render as a washed tan fill carrying near-white text, the lowest contrast pair of objects anywhere on the page, because the disabled treatment is a 50 percent opacity wash over gold `#ad8c3a` on parchment. The two controls that carry the whole demo read as broken rather than as waiting. Same at 390px in `before-mobile-page-3.png` and `before-mobile-page-4.png`. | source: `components/ui/button.tsx:8` (`disabled:opacity-50` in the cva base, applied to the `default` gold variant) | violates: IDENTITY.md ACCENT gold as "the one primary control"; an opacity wash is not a token | fixed by: C7
5. SHOT `before-desktop-page-2.png` | region: "Send edited plan", right of the tamper field | defect: the refusal control renders as pale pink with white text, quieter than the gold hairlines above it, so the loudest intended moment on the page is the faintest control on it. Same at 390px in `before-mobile-page-4.png`, where it sits above an equally washed gold button. | source: `components/ui/button.tsx:16` (`destructive` variant, `bg-destructive text-destructive-foreground`, washed further by the same `disabled:opacity-50`) | violates: IDENTITY.md SECOND oxide-red `#9c3b2a`, spent "only on the refused row, so the moment the treasury key rejects a tampered payout is the single loudest thing on screen" | fixed by: C8
6. SHOT `before-desktop-page-2.png` | region: the "AMOUNT FOR MARMARA PENSION FUND II" field | defect: the real value `7756.25` renders in the muted grey the placeholder uses, inside a transparent field on parchment, so the one number the judge is about to edit reads as a placeholder in an empty box. Same tone at 390px in `before-mobile-page-4.png`. | source: `components/ui/input.tsx:9` (`bg-transparent` with no explicit foreground, so the field inherits and the value is indistinguishable from `placeholder:text-muted-foreground`) | violates: IDENTITY.md INK `#191713` for read values; DEMO.md step 4 needs that number legible on camera | fixed by: C9
7. SHOT `before-desktop-page-2.png` | region: the "NO POLICY INSTALLED" panel in the policy card and the "NOTHING RECORDED YET" panel under Audit record | defect: both are drawn with a dashed bone-shadow `#e2dccd` outline, the only dashed strokes anywhere in the product, reading as a dropzone or a placeholder rather than as a register that is currently empty. Also at 390px in `before-mobile-page-3.png` and `before-mobile-page-4.png`. | source: `components/console-states.tsx:208, 222, 235, 273` (`border border-dashed border-border`, four sites) | violates: IDENTITY.md D11 museum, "wide margins, hairline rules"; a dashed border is not a ledger treatment | fixed by: C10
8. SHOT `before-desktop-fold.png` | region: the "CORPORATE ACTION" row between the masthead rule and the plan card | defect: the gold selected button and the white unselected button sit loose on the ground beside the label with the same 12px gap that separates the label from them, so the pair does not read as one control with two states. At 390px in `before-mobile-page-1.png` it is worse: the label and the gold button share a row, the second option wraps onto its own line below, and the two look like unrelated controls. | source: `components/operations-console.tsx:464` (`flex flex-wrap items-center gap-3 border-t border-border pt-8`, no group element, no `aria-pressed`) | violates: IDENTITY.md "wide-tracked small caps over each column"; a control group needs its own label and its own spacing | fixed by: C6
9. SHOT `before-desktop-page-3.png` | region: the `#brief` fold, "Start at the register" and the closing sponsor sentence | defect: the outline CTA sits mid-prose between the lead paragraph and the "Why this exists" heading, with the same 24px rhythm above and below, so it reads as a stray control rather than the way into DEMO step 1. The footer below carries one sponsor sentence in muted grey and no link at all, so a judge who reaches the bottom of the page has nowhere to go. | source: `app/page.tsx:33` (the `Button` between two siblings of a flat `space-y-6` section) and `app/page.tsx:74` (footer with a single `<p>`) | violates: the phase goal of one primary way into DEMO step 1; nothing in IDENTITY.md asks for a control buried in running text | fixed by: C11, C12
10. SHOT `before-mobile-page-2.png` | region: the plan table at 390px | defect: only the Holder and Account columns are on screen; Position, Coupon due and Status sit off the right edge behind the horizontal scroll of the 46rem minimum width grid, so the three oxide-red held rows show their reasons but not their struck-through amounts without a sideways drag. | source: `components/operations-console.tsx:491` (`overflow-x-auto` wrapper) and `:492` (`min-w-[46rem]`) | violates: nothing in IDENTITY.md; it is a legibility cost, not a contract breach | Kept (see Kept, row 1)

## Changes

| id | claim (one sentence) | file | proof-gone | proof-new |
| --- | --- | --- | --- | --- |
| C1 | The rail blurb is held behind the `lg` breakpoint so the phone first screen spends no height on a three line description of the product. | `components/rail.tsx` | `max-w-[28ch] text-sm leading-relaxed text-muted-foreground` (0 matches after) | `hidden max-w-[28ch] text-sm leading-relaxed` (1 match) |
| C2 | The six section links become one horizontally scrollable row inside its own `overflow-x-auto` wrapper below `lg`, and the same stacked column at `lg` and up, so they cost one row of height instead of two and never scroll the page sideways. | `components/rail.tsx` | `flex flex-wrap gap-x-6 gap-y-2 border-t border-border pt-6` (0 matches after) | `overflow-x-auto border-t border-border` (1 match) |
| C3 | The About and Security `dl` leaves the rail's mobile first screen and is rendered once in the shared shell below the console, so all four rows and the SECURITY.md link survive at 360px on `/` and on `/record/[planHash]` alike. | `components/rail.tsx`, `app/layout.tsx` | `space-y-2 border-t border-border pt-5 text-xs` (0 matches after) | `<AboutSecurity />` (2 matches: `components/rail.tsx:147`, `app/layout.tsx:66`) |
| C4 | The ledger rule comes off the plan card header, so the display-face title and its description are no longer struck through, and a wide-tracked eyebrow carries the header instead. | `components/operations-console.tsx` | `detent-ruled border-b border-border` (0 matches after) | `Plan, as it will be signed` (1 match) |
| C5 | The register note is demoted from a filled parchment panel to hairline-ruled rows on the ground colour at smaller type, so the security name is unambiguously the first read on the fold. All four rows, `snapshot.note` and `detent-enter` are kept. | `components/operations-console.tsx` | `detent-enter border border-border bg-card p-4` (0 matches after) | `detent-enter border-t border-border pt-3` (1 match) |
| C6 | The corporate action picker becomes a labelled control group: the wide-tracked label on its own line, the two options in a `role="group"` with `aria-pressed`, the unselected one on a gold hairline over the ground so it reads as an available alternative rather than a disabled card. | `components/operations-console.tsx` | `flex flex-wrap items-center gap-3 border-t border-border pt-8` (0 matches after) | `aria-label="Corporate action"` (1 match, `components/operations-console.tsx:472`) |
| C7 | The disabled gold primary stops being a 50 percent wash and becomes a bordered, unfilled, muted-foreground control, so "Lock this plan to the treasury key" and "Execute the approved plan" stay readable while they wait. `disabled:pointer-events-none`, `rounded-none` and every `min-h` survive. | `components/ui/button.tsx` | `disabled:pointer-events-none disabled:opacity-50` (0 matches after) | `disabled:border disabled:border-border disabled:bg-transparent` (1 match) |
| C8 | The refusal control becomes the loudest control on the page: a full oxide-red fill with a 2px oxide rule and ground ink, set in wide-tracked capitals, and even in its disabled state it keeps the oxide rule and oxide ink instead of fading to pink. | `components/ui/button.tsx` | `bg-destructive text-destructive-foreground hover:bg-destructive/90` (0 matches after) | `border-2 border-destructive bg-destructive font-semibold uppercase` (1 match) |
| C9 | The tamper field gets an explicit ink foreground on a `bg-card` field, so the typed value reads as an editable cell rather than as placeholder text. `placeholder:text-muted-foreground` stays. | `components/ui/input.tsx` | `bg-transparent px-3 py-1 text-sm transition-colors` (0 matches after) | `bg-card px-3 py-2 text-sm text-foreground` (1 match) |
| C10 | All four dashed empty states become the hairline rule idiom already in use, same copy, same `detent-label` heading, same closing `Button`. | `components/console-states.tsx` | `border border-dashed border-border` (0 matches after) | `border-y border-border bg-card px-6 py-10 text-center` (2 matches: `components/console-states.tsx:210, 237`; the other two sites became `border border-border bg-card p-6`) |
| C11 | The `#brief` lead and its CTA become one lead block that ends in the control, so there is exactly one way into DEMO step 1 and it is not buried between two paragraphs. | `app/page.tsx` | `id="brief" className="max-w-[68ch] space-y-6 border-t border-border pt-10"` (0 matches after) | `id="brief" className="max-w-[68ch] space-y-8` (1 match) |
| C12 | The footer carries the repo link and the hackathon link beside the sponsor sentence, both `target="_blank" rel="noopener noreferrer"`, so the bottom of the page is an exit rather than a dead end. | `app/page.tsx` | `<footer className="border-t border-border pt-8 text-sm leading-relaxed text-muted-foreground">` (0 matches after) | `https://ethglobal.com/events/ethonline2026` (1 match) |

## Kept

1. Diagnosis 10, the plan table's sideways scroll at 390px. The register keeps its five columns and its `min-w-[46rem]` grid inside the existing `overflow-x-auto` wrapper rather than collapsing to stacked cards, because a transfer agent's ledger is a table and because the number the judge edits in DEMO step 4 is printed again, at full size and in ink, in the tamper field of the send section. Rebuilding the table as a responsive card list is a structural change to the one surface the demo depends on, which is not worth spending inside this budget. Picked up at `components/operations-console.tsx:491`.

## Grep results recorded at the end of the phase

- Dead hex and banned keyframes, over `app/` and `components/`, pattern `0b0f14|080b11|0f172a|0a0f1a|10151f|111820|1e293b|1e2a36|212b3d|2dd4bf|5eead4|14b8a6|0d9488|99f6e4|34d399|22d3ee|67e8f9|06b6d4|0891b2|38bdf8|0ea5e9|f5a524|fbbf24|f59e0b|fade-up|float-y|glow-pulse|caret-blink|pulse-dot`: **0 hits**.
- Banned shapes, over `app/` and `components/`: `font-mono` **0**, `role="tablist"` **0**, `animate-pulse` **0**, `--delay` **0**, `--d:` **0**, `rounded-xl` **0**, `backdrop-blur` **0**, `mask-image` **0**, `border-dashed` **0**.
- Hex literals outside `app/globals.css`, over `app/` and `components/`: **4 lines, all in `app/icon.svg`** (`#f4f1ea`, `#ece7dc`, `#ad8c3a`, `#9c3b2a`), which is the pre-existing favicon convention file and not a component. An SVG served as its own document cannot read the custom properties in `app/globals.css`, and the four values are IDENTITY.md's own. Zero hits in any `.tsx`. The values in `app/globals.css` still read `#f4f1ea #ece7dc #e2dccd #191713 #ad8c3a #9c3b2a #2e7d4f #bf8477`.
- Bare controls outside `components/ui/*`: `<button` **0**, `<select` **0**, `<input` **1** and it is the primitive itself at `components/ui/input.tsx:6`. `rounded-full` **0**.
- `detent-ruled` in `components/operations-console.tsx`: **0**. The class survives in `app/globals.css` only, where it is declared.
- Brand images: `<Image` in `components/rail.tsx` **1**, inside the home `Link`. `brand/og.png` under `components/` **0**.
