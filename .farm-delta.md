# DELTA (phase 9)

Read first: `IDENTITY.md` (D11 museum, L6 rail, M4 wipe, RADIUS sharp) and all
nine BEFORE shots under
`/Users/mericcintosun/farm-hackathon/data/exports/ethglobal-ethonline-2026/shots-faz9-mtxwk6ml/`.
Every change below moves existing markup onto tokens that already exist. No new
colour, font, radius or motion value is introduced anywhere in this phase: the
one keyframe stays `detent-wipe` and the scroll-driven band reuses it.

## One-mark proof

`public/brand/logo.png` is the mark and `components/rail.tsx` renders it exactly
once, inside the home `Link`, as one `<Image>` plus one text node:

```tsx
<Link
  href="/"
  aria-current="page"
  className="flex items-center gap-3 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
>
  <Image
    src="/brand/logo.png"
    alt=""
    width={44}
    height={44}
    priority
    className="h-11 w-11 object-contain"
  />
  <span className="font-display text-2xl tracking-tight">Detent</span>
</Link>
```

No second brand image exists in the file. The five plates added this phase are
section visuals, not marks: none of them is rendered in `components/rail.tsx` or
in the register masthead, and each one carries `alt=""` with `aria-hidden`.

## Diagnosis

1. SHOT `before-desktop-fold.png` | region: the persistent left rail, the six section labels from "REGISTER" down to "WHY IT EXISTS" | defect: all six wide-tracked labels render in the same muted grey (ink `#191713` mixed 60 percent into bone `#f4f1ea`), identically, whether or not the reader is looking at that section. The column that IDENTITY.md calls the product carries no gold `#ad8c3a` anywhere and reports no position, so scrolling the console changes nothing in the shell. Same at 390px in `before-mobile-fold.png`, where the scrolling label row reads as six equal words. | source: `components/rail.tsx:131` (`detent-label inline-flex min-h-11 shrink-0 items-center whitespace-nowrap transition-colors hover:text-foreground`, the only class the six links carry) | violates: IDENTITY.md ARCHETYPE L6 rail, "it is the operator's console, so the shell itself is the product" | fixed by: C1
2. SHOT `before-desktop-fold.png` | region: the masthead group, the eyebrow "ERC-1400 EQUITY (ATS) · 2026-Q3 WINDOW", the display-face h1 "Bosphorus Mercantile Equity", the lede and the badge row | defect: the only object on the whole first screen that animates is the "REGISTER NOTE" list at the top right, printed at 11px in muted grey; the eyebrow, the h1 in ink `#191713` and the two badges arrive with no entrance at all. On camera the fold's smallest and greyest block is the one that moves and the name of the security does not. Same order at 390px in `before-mobile-fold.png`. | source: `components/operations-console.tsx:428` (`detent-enter border-t border-border pt-3` on the register note) against `components/operations-console.tsx:389, 393, 396, 403`, four siblings carrying no motion class | violates: IDENTITY.md MOTION M4 wipe, whose whole job is the first read | fixed by: C2, C8
3. SHOT `before-desktop-page-2.png` | region: the "Compiled wallet policy" card, and the "PLAN HASH / CALL" row roughly 900 pixels above it | defect: the four pinned conditions are four wide-tracked grey terms over truncated hex on parchment `#ece7dc`, and nothing on screen connects `data eq 0xca4ead79…` to the call `distributeCoupon(bytes32,address[],uint256[])` printed in the plan card far above. The judge is asked to believe the pin rather than see it, and neither hover nor keyboard gives them a way to look. | source: `components/operations-console.tsx:730-745`, the `dl` mapping `installation.policy.rules[0].conditions` with a static truncation at `:740` | violates: DEMO.md step 3, which says the card "shows the four pinned conditions" and is the moment the Privy bounty is scored on | fixed by: C5
4. SHOT `before-desktop-page-3.png` | region: the `#brief` fold below the console, from "Detent is the operator console" to the end of "How it differs from a simulator" | defect: the page ends in roughly 450 words of running text in ink-at-60-percent grey on bone `#f4f1ea`, four stacked paragraphs with no object in any of them; the simulator paragraph alone runs nine lines wide. At 390px in `before-mobile-page-4.png` that same prose fills two entire screens, so the last thing a judge sees on a phone is an essay. The closing sponsor sentence under it runs three lines for information the rail already carries. | source: `app/page.tsx:29-35` (the lead), `app/page.tsx:63-75` (the simulator block), `app/page.tsx:79-83` (the footer paragraph) | violates: IDENTITY.md D11 museum, "wide margins, hairline rules ... the way a transfer agent's ledger page reads"; a wall of prose is neither | fixed by: C3, C4, C6
5. SHOT `before-desktop-page-2.png` | region: the "NO POLICY INSTALLED" panel in the policy card and the "NOTHING RECORDED YET" panel under "Audit record" | defect: both are wide hairline bands of parchment `#ece7dc` holding two lines of muted grey text and nothing else, roughly 120 and 100 pixels of empty surface sitting in the middle of the console at exactly the two moments the judge is waiting (before the lock, before the send). Same two blanks at 390px in `before-mobile-page-3.png` and `before-mobile-page-4.png`. | source: `components/console-states.tsx:210` (`PlanEmptyState`) and `components/console-states.tsx:237` (`LedgerEmptyState`) | violates: IDENTITY.md D11 museum; an engraved ledger page is not blank where it is waiting | fixed by: C7

## Changes

| id | claim (one sentence) | file | proof-gone | proof-new |
| --- | --- | --- | --- | --- |
| C1 | The six section links move into a client `SectionProgress` that renders the same six anchors in server HTML and marks the section currently in view with `aria-current="location"`, full-ink text and a gold hairline, so the rail reports position instead of listing it. | `components/rail.tsx`, `components/section-progress.tsx` | `shrink-0 items-center whitespace-nowrap transition-colors` (0 matches after) | `<SectionProgress` (1 match) |
| C2 | The masthead group becomes a `detent-stagger` container whose eyebrow, h1, lede and badge row each carry `detent-enter`, so the first screen wipes in per M4 and the security's name leads the entrance instead of the register note. | `components/operations-console.tsx` | `="max-w-[16ch] text-4xl leading-[1.05] tracking-tight sm:text-5xl"` (0 matches after) | `detent-stagger space-y-4` (1 match) |
| C3 | The `#brief` lead collapses to one sentence plus the existing outline Button, and three one-sentence steps sit on a ruled band with a plate each, which also gives `.detent-ruled` and `public/illustrations/ledger-rule.svg` the home Phase 8 left open. | `app/page.tsx` | `Detent is the operator console for a tokenized security. It is built` (0 matches after) | `detent-ruled flex items-center justify-center` (1 match; the strip that holds it carries `detent-band`, 1 match on the same file) |
| C4 | The simulator prose folds behind a one line disclosure carrying a compact four row comparison, so the same claim is checkable in a table instead of a nine line paragraph. | `app/page.tsx` | `Tenderly and the Safe integration built on it show you the outcome` (0 matches after) | `<summary` (1 match) |
| C5 | The policy card gains an explorable diagram: the real call is printed as one wrapped strip and each condition is a real `<button>` that, on hover and on focus, highlights the fragment of that call it pins and glosses it in one plain sentence. | `components/operations-console.tsx`, `components/policy-explorer.tsx` | `{condition.value.length > 66` (0 matches after) | `<PolicyExplorer` (1 match) |
| C6 | The footer drops to one sentence beside its two existing links, so the bottom of the page is an exit rather than a third paragraph of sponsor prose. | `app/page.tsx` | `Tokenization Studio for the security, Privy server wallets, policies` (0 matches after) | `Built for ETHOnline 2026 on Hedera testnet, chain 296.` (1 match) |
| C7 | The two centred empty panels stop being blank surface: each carries its own engraved plate above the same `detent-label` heading and the same sentence. | `components/console-states.tsx` | `="border-y border-border bg-card px-6 py-10 text-center"` (0 matches after) | `/brand/plate-register.svg` (1 match) |
| C8 | One new CSS block, a scroll-driven band reveal running the existing `detent-wipe` on `animation-timeline: view()` inside an `@supports` guard, and the reduced-motion block is extended to cover it. | `app/globals.css` | `.detent-stagger > * {` (0 matches after) | `@supports (animation-timeline: view())` (1 match) |

## Kept

1. Diagnosis 4's middle paragraph, "Why this exists". The two paragraphs under that heading stay as running text: they are the only place on the site that states the problem in the operator's own words, DEMO.md's "The problem" derives from them, and cutting them would leave the page with a product and no reason. Only the lead, the simulator block and the footer are rebuilt.
2. The plan table's sideways scroll at 390px, carried verbatim from the Phase 8 delta. The register keeps its five columns and its `min-w-[46rem]` grid inside the existing `overflow-x-auto` wrapper at `components/operations-console.tsx`, because a transfer agent's ledger is a table and the number the judge edits in DEMO step 4 is printed again, at full size and in ink, in the tamper field.

## Grep results recorded at the end of the phase

- Every phase 9 `proof-gone` above, over the repo with `.farm-delta.md` and `HANDOFF.md` excluded: **0 matches each**. Every `proof-new`: **at least 1 match each**, counts as written in the table.
- Every phase 8 `proof-gone` string re-run over the repo: **11 of 12 return 0**. The exception is `max-w-[28ch] text-sm leading-relaxed text-muted-foreground`, which returns **1** at `components/rail.tsx:113`. That is not a regression: Phase 8's own replacement for the row was `hidden max-w-[28ch] text-sm leading-relaxed`, which contains the old string as a substring, so the row could never have returned zero on the day it was written. The blurb is still held behind `lg`, which is what Phase 8 claimed. The class string was not reordered to make the grep pass. The eleven that do return 0: `flex flex-wrap gap-x-6 gap-y-2 border-t border-border pt-6`, `space-y-2 border-t border-border pt-5 text-xs`, `detent-ruled border-b border-border`, `detent-enter border border-border bg-card p-4`, `flex flex-wrap items-center gap-3 border-t border-border pt-8`, `disabled:pointer-events-none disabled:opacity-50`, `bg-destructive text-destructive-foreground hover:bg-destructive/90`, `bg-transparent px-3 py-1 text-sm transition-colors`, `border border-dashed border-border`, `id="brief" className="max-w-[68ch] space-y-6 border-t border-border pt-10"`, `<footer className="border-t border-border pt-8 text-sm leading-relaxed text-muted-foreground">`: nothing Phase 8 fixed came back.
- Dead hex and banned keyframes, over `app/` and `components/`, pattern `0b0f14|080b11|0f172a|0a0f1a|10151f|111820|1e293b|1e2a36|212b3d|2dd4bf|5eead4|14b8a6|0d9488|99f6e4|34d399|22d3ee|67e8f9|06b6d4|0891b2|38bdf8|0ea5e9|f5a524|fbbf24|f59e0b|fade-up|float-y|glow-pulse|caret-blink|pulse-dot`: **0 hits**.
- Banned shapes, over `app/` and `components/`: `font-mono` **0**, `role="tablist"` **0**, `animate-pulse` **0**, `--delay` **0**, `--d:` **0**, `rounded-xl` **0**, `rounded-full` **0**, `backdrop-blur` **0**, `mask-image` **0**, `border-dashed` **0**, `CopyChip` **0**, `Reveal` **0**.
- Hex literals outside `app/globals.css`, over `app/` and `components/`: **4 lines, all in `app/icon.svg`** (`#f4f1ea`, `#ece7dc`, `#ad8c3a`, `#9c3b2a`), the pre-existing favicon convention file, restated in `HANDOFF.md`. **0 in any `.tsx`.** The five plates under `public/brand/` carry IDENTITY.md's own six values and are not under `app/` or `components/`.
- Keyframes in `app/globals.css`: `@keyframes` **1**, and it is `detent-wipe`. `animation-timeline: view()` appears once, inside `@supports`.
- Brand images: `<Image` in `components/rail.tsx` **1**, inside the home `Link`. `brand/og.png` under `components/` **0**. `brand/plate-` under `components/rail.tsx` **0**.
