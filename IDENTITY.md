# IDENTITY (contract; every later phase obeys this file)

DIRECTION: D11 museum
GROUND: #f4f1ea bone
SURFACE: #ece7dc parchment
LINE: #e2dccd bone-shadow
INK: #191713
ACCENT: #ad8c3a gold
SECOND: #9c3b2a oxide-red
OK: #2e7d4f
WARN: #bf8477
BAD: #9c3b2a
FONT_DISPLAY: F8 Libre Caslon Text
FONT_BODY: Libre Franklin
FONT_MONO: none
ARCHETYPE: L6 rail
MOTION: M4 wipe, prefix detent-
RADIUS: sharp
HEADER: solid ground, border-r hairline, persistent left rail
LOGO_POLICY: raster-only

## Why

A shareholder register is a record, so the product is dressed as one: wide margins, hairline rules, wide-tracked small caps over each column, the way a transfer agent's ledger page reads. Gold is spent on the rules and the one primary control, and oxide red is spent only on the refused row, so the moment the treasury key rejects a tampered payout is the single loudest thing on screen. The rail archetype fits because Detent is not a landing page with a demo behind it, it is the operator's console, so the shell itself is the product.

## Amendments (append-only; later phases add dated lines here, never edit the block above)

2026-09-12, Phase 2. The send section's failure surface is now the same oxide-red bordered paragraph carrying one hint sentence, with the plan blockers listed under it as plain oxide-red lines when the error code is plan_blocked. No new colour, radius, font or motion value: the list reuses the treatment the plan table already gives its blockers.

2026-09-12, Phase 3. One new recurring surface, the treasury key banner at the top of the send section: a bordered block carrying a detent-label line over one paragraph, bordered border-border while the key is resting or waiting, border-ok when the payload was signed and border-bad only on the refusal. No new colour, radius, font or motion value: it is the treatment the settlement block already uses, given a fixed place so the key's state does not appear and disappear. The empty states and the send failure now end in an outline Button rather than a sentence.

2026-09-12, Phase 4. The console masthead no longer carries a second raster. `public/brand/og.png` is gone from `components/operations-console.tsx` and a bordered register note block takes its place: `border border-border bg-card p-4`, a `detent-label` heading, a `dl` carrying the snapshot time, the register source, the partition and the settlement asset, ending in `snapshot.note`, and it keeps `detent-enter`. LOGO_POLICY is now satisfied strictly: exactly one brand mark per page, inside the rail's brand link. No new colour, radius, font or motion value, since every treatment here is one the console already used.

2026-09-12, Phase 5. Two recurring surfaces, both reusing treatments the shell already had. First, the send disclosure block under the treasury key banner in the send section: `border border-border bg-card p-4`, a `detent-label` heading over one sentence naming the call, the row count, the draw and the chain, ending in a row that carries the destination contract truncated and linked plus a ghost Button copy control. It is the register note block's treatment, given a second home. Second, the rail's network `dl` is now the About and Security block: the same `detent-label` terms, small muted text and hairline underlined links, with rows added for the chain id, the anchor contract and the security policy link. No new colour, radius, font or motion value, and still exactly one brand mark per page.

2026-09-12, Phase 8. Four recurring treatments changed, none of them a new value. First, the rail collapses below `lg` to a compact bar: the mark on one row, the six section links as one horizontally scrollable row inside its own `overflow-x-auto` wrapper, the blurb and the CTA held behind `lg`, and the About and Security `dl` rendered once in the shared shell below the console so it still reaches `/` and `/record/[planHash]` at 360px. At `lg` and up the rail is unchanged: solid ground, `border-r` hairline, persistent, sticky, full height. Second, the register note is demoted from `border border-border bg-card p-4` to `border-t border-border` with `divide-y divide-border` rows at `text-xs` on the ground colour, so the h1 is the first read on the fold; it keeps all four rows, `snapshot.note` and `detent-enter`. Third, two control states: a disabled `default` Button is now a bordered unfilled control on `border-border` with `muted-foreground` ink instead of a 50 percent wash over gold, and the `destructive` Button is a full oxide-red fill on a 2px oxide rule with ground ink in wide-tracked capitals, keeping the oxide rule and oxide ink when disabled, so the refusal is the loudest control on the page. Fourth, the four dashed empty states become the hairline idiom the rest of the product uses (`border-y border-border bg-card` for the two centred panels, `border border-border bg-card` for the two blocks). The ledger rule also comes off the plan card header, where it was striking through the title and the description; it stays declared in `app/globals.css` as a surface. No new colour, font, radius or motion value was introduced: every class above resolves to a token already in this file.

2026-09-12, Phase 9. Six recurring treatments, one new class and no new colour, font, radius or keyframe. First, the rail reports position: the section currently in view carries `aria-current="location"` and `text-foreground`, plus a gold left hairline at `lg` and up (`lg:border-l-2 lg:border-hairline`, `lg:border-transparent` at rest, `lg:pl-3` so nothing shifts when it lights). The six links keep the mobile `overflow-x-auto` row and the `lg` stacked column exactly as Phase 8 left them; only the marking is new, and it lives in `components/section-progress.tsx`. Second, the staged fold: the masthead's eyebrow, h1, lede and badge row sit in a `detent-stagger` container and each carries `detent-enter`, so the first screen arrives on the existing nth-child delays and never through an inline `--delay`. Third, one new class, `.detent-band`: the same `detent-wipe` driven by the element's own scroll progress, with the whole rule body inside `@supports (animation-timeline: view())` so it is inert where unsupported, and added to the `prefers-reduced-motion: reduce` block beside `detent-enter` and the stagger children. `app/globals.css` still declares exactly one keyframe. Fourth, a family of five engraved plates under `public/brand/` (`plate-plan`, `plate-policy`, `plate-refusal`, `plate-register`, `plate-record`): flat matte ledger shapes on the six values above, square corners, no gradient and no glow, rendered only through `components/plates.tsx` with `alt=""` and `aria-hidden="true"`. They are section visuals, not marks. LOGO_POLICY is unchanged: none of them renders in `components/rail.tsx` or in the register masthead, and `public/brand/logo.png` inside the rail's home `Link` is still the single brand mark per page. Fifth, the explorable condition row in the policy card: a ghost `Button` block that takes a gold left hairline and `bg-accent` while it is hovered or focused, and the fragment of the call it pins takes `bg-secondary` in the strip above while the rest of that strip drops to `muted-foreground`. Sixth, the ledger rule finds the home Phase 8 left open: `detent-ruled` now sits behind the three plates of the `#brief` strip and under no running text at all, and the prose it displaced folds behind a `<details>` whose `<summary>` is a `detent-label`. Every class above resolves to a token already in this file.

2026-09-12, Phase 6. The masthead reorders and gains one line, both inside the existing `detent-stagger` container in `components/operations-console.tsx`, and no new colour, font, radius or motion value is introduced. First, a status line is now the container's first child, a `detent-enter detent-label` paragraph carrying the register source, the treasury key mode and the chain id joined by the same middle dot the eyebrow uses, with the Privy policy id appended once a plan is locked. It is the label treatment the console already used, given the first position on the fold, so a reader learns which mode the screen is running in before reading anything it claims. Second, the `h1` is now the product name, `Detent`, in `font-display` on the size and tracking classes it already carried, followed by a one-sentence promise at `text-lg` and then the security name in the eyebrow's `detent-label` treatment, so `snapshot.token.name` moves down two positions rather than disappearing. The container now holds seven children, which the existing nth-child delays already cover through the `n + 7` clause; no inline delay was added. Third, the compiled policy card prints the policy id as a `detent-label` term over a `text-sm` value, the same term-and-value pairing the register note uses. LOGO_POLICY is unchanged: no raster was added, and `public/brand/logo.png` inside the rail's home `Link` is still the single brand mark per page.

2026-09-12, Phase 7. Three recurring treatments now state their provenance, and no new colour, font, radius or motion value is introduced. First, the fold's status line always ends on the policy slot: `No policy locked yet` before a lock, then `Privy policy <id>` only when Privy issued the id and `Policy <id>, compiled locally` otherwise, so the line never lends a local id a sponsor's name. Second, the treasury key banner's `tx-rejected` branch carries one extra `text-sm text-muted-foreground` sentence naming the engine that refused, the Privy server wallet or the local mirror of the same evaluator; it is the muted sentence the branch already had, given a second instance, and the `Frame` tones stay `neutral | bad | ok`. Third, the settlement badge row gains one `border-border text-muted-foreground` outline badge, `refused by: …`, in the same lower-case idiom as the `policy source: …` badge beside it and shown only on a refusal, and the fold's address row splits on `snapshot.source`: the live read keeps the `border-ok` verified badge and the HashScan link, while the cached register carries a `border-border text-muted-foreground` badge reading `Seed register address, not on chain` over the same truncated address as plain text with no link, because a link to an explorer page that does not exist is worse than no link. LOGO_POLICY is unchanged: no raster was added, and `public/brand/logo.png` inside the rail's home `Link` is still the single brand mark per page.

2026-09-12, refactor pass. The `DIFFERS_FROM` key is removed from the block
above. It recorded which design axes were ruled out by comparison with unrelated
projects, which says nothing about this product to a reader of this repository.
No token, treatment or rule changed: the block still declares the same ground,
ink, accent, type, archetype, radius and motion values every component resolves
against.

2026-09-12, UI defect pass. Contrast only, no new value. `--primary-foreground` is now ink instead of ground, because ground on gold measured 2.8:1 and ink on gold is 5.6:1. `--muted-foreground` mixes 70 percent ink into the ground instead of 60, so muted text and every `detent-label` clear 4.5:1 on ground, surface and surface-deep. `--ring` is gold deepened with 30 percent ink so the focus ring clears 3:1. OK moves from `#2e7d4f` to `#256a42`, because the signed state printed OK text at 4.1:1 on the surface; the new value clears 4.5:1 on ground, surface and surface-deep. Two treatments change with no new value: below `lg` the rail's six section links wrap onto a second line instead of scrolling, and the plan table's status column is pinned to the right edge of its own scroll wrapper, with a `detent-label` line saying the table scrolls sideways whenever it does. The `.detent-band` scroll wipe moves from the `#brief` step list onto each step's plate frame, so the running text beside a plate is never held at the wipe's 35 percent opacity.
